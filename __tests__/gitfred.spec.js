const gitfred = require('../gitfred');

describe('Given the gitfred library', () => {
  describe('when initializing the library', () => {
    it('should create something', () => {
      expect(1).toEqual(1);
    });
  });
});